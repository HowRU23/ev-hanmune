# tools/

아직은 비어있음. 이 프로젝트는 지금 순수 콘텐츠 웹사이트(Next.js)라서 외부 API 호출이나 데이터 자동 처리가 필요 없음.

나중에 아래 같은 필요가 생기면 여기에 파이썬 스크립트로 추가:
- 전기차 모델/가격 데이터를 외부 API나 공공데이터에서 가져와야 할 때
- 콘텐츠 초안을 자동 생성/변환해야 할 때
- 이미지 최적화, 통계 수집 등 반복 작업이 필요할 때

새 Tool을 만들기 전에 이미 비슷한 게 없는지 먼저 확인할 것 (CLAUDE.md 규칙).
